---
title: Complete embedding CLI command reference
description: Exact syntax and effects for every registered embedding command.
order: 8
category: Reference
---

This page is generated from the same machine-readable authority used by CLI help. Do not edit it manually.

| Command | Exact syntax | Required inputs | Configuration | I/O | Mutation/overwrite | Network/cost | Error families | Contract/version |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `embed data create` | `finetuning embed data create [input\|-] [--out <path\|->] [--json] [--dry-run] [--force]` | input or stdin when the operation consumes rows | mapping, provenance, split/dedupe flags | input/stdin; --out/stdout only when declared | writes explicit output/state; overwrite requires --force or resume | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed data import` | `finetuning embed data import [input\|-] [--out <path\|->] [--json] [--dry-run] [--force]` | input or stdin when the operation consumes rows | mapping, provenance, split/dedupe flags | input/stdin; --out/stdout only when declared | writes explicit output/state; overwrite requires --force or resume | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed data convert` | `finetuning embed data convert [input\|-] [--out <path\|->] [--json] [--dry-run] [--force]` | input or stdin when the operation consumes rows | mapping, provenance, split/dedupe flags | input/stdin; --out/stdout only when declared | writes explicit output/state; overwrite requires --force or resume | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed data validate` | `finetuning embed data validate [input\|-] [--out <path\|->] [--json] [--dry-run] [--force]` | input or stdin when the operation consumes rows | mapping, provenance, split/dedupe flags | input/stdin; --out/stdout only when declared | read-only unless selected execution is explicitly run | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed data inspect` | `finetuning embed data inspect [input\|-] [--out <path\|->] [--json] [--dry-run] [--force]` | input or stdin when the operation consumes rows | mapping, provenance, split/dedupe flags | input/stdin; --out/stdout only when declared | read-only unless selected execution is explicitly run | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed data split` | `finetuning embed data split [input\|-] [--out <path\|->] [--json] [--dry-run] [--force]` | input or stdin when the operation consumes rows | mapping, provenance, split/dedupe flags | input/stdin; --out/stdout only when declared | writes explicit output/state; overwrite requires --force or resume | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed data dedupe` | `finetuning embed data dedupe [input\|-] [--out <path\|->] [--json] [--dry-run] [--force]` | input or stdin when the operation consumes rows | mapping, provenance, split/dedupe flags | input/stdin; --out/stdout only when declared | writes explicit output/state; overwrite requires --force or resume | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed data freeze` | `finetuning embed data freeze [input\|-] [--out <path\|->] [--json] [--dry-run] [--force]` | input or stdin when the operation consumes rows | mapping, provenance, split/dedupe flags | input/stdin; --out/stdout only when declared | writes explicit output/state; overwrite requires --force or resume | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed data export` | `finetuning embed data export [input\|-] [--out <path\|->] [--json] [--dry-run] [--force]` | input or stdin when the operation consumes rows | mapping, provenance, split/dedupe flags | input/stdin; --out/stdout only when declared | writes explicit output/state; overwrite requires --force or resume | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed generate queries` | `finetuning embed generate queries [--limit <n>] [--json] [--dry-run]` | versioned --config; operation-specific checkpoint/artifact/report where shown | CLI > environment reference > config > default | one JSON result on stdout; progress on stderr | read-only unless selected execution is explicitly run | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed generate documents` | `finetuning embed generate documents [--limit <n>] [--json] [--dry-run]` | versioned --config; operation-specific checkpoint/artifact/report where shown | CLI > environment reference > config > default | one JSON result on stdout; progress on stderr | read-only unless selected execution is explicitly run | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed generate pairs` | `finetuning embed generate pairs [--limit <n>] [--json] [--dry-run]` | versioned --config; operation-specific checkpoint/artifact/report where shown | CLI > environment reference > config > default | one JSON result on stdout; progress on stderr | read-only unless selected execution is explicitly run | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed mine negatives` | `finetuning embed mine negatives [--limit <n>] [--json] [--dry-run]` | versioned --config; operation-specific checkpoint/artifact/report where shown | CLI > environment reference > config > default | one JSON result on stdout; progress on stderr | read-only unless selected execution is explicitly run | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed distill vectors` | `finetuning embed distill vectors --config <path> [--input <path>] [--state <path>] [--json] [--dry-run]` | versioned --config; operation-specific checkpoint/artifact/report where shown | CLI > environment reference > config > default | one JSON result on stdout; progress on stderr | read-only unless selected execution is explicitly run | provider/network and separate budgets required unless offline fixture | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed distill scores` | `finetuning embed distill scores --config <path> [--input <path>] [--state <path>] [--json] [--dry-run]` | versioned --config; operation-specific checkpoint/artifact/report where shown | CLI > environment reference > config > default | one JSON result on stdout; progress on stderr | read-only unless selected execution is explicitly run | provider/network and separate budgets required unless offline fixture | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed distill rankings` | `finetuning embed distill rankings --config <path> [--input <path>] [--state <path>] [--json] [--dry-run]` | versioned --config; operation-specific checkpoint/artifact/report where shown | CLI > environment reference > config > default | one JSON result on stdout; progress on stderr | read-only unless selected execution is explicitly run | provider/network and separate budgets required unless offline fixture | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed distill plan` | `finetuning embed distill plan --config <path> [--input <path>] [--state <path>] [--json] [--dry-run]` | versioned --config; operation-specific checkpoint/artifact/report where shown | CLI > environment reference > config > default | one JSON result on stdout; progress on stderr | read-only unless selected execution is explicitly run | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed distill run` | `finetuning embed distill run --config <path> [--input <path>] [--state <path>] [--json] [--dry-run]` | versioned --config; operation-specific checkpoint/artifact/report where shown | CLI > environment reference > config > default | one JSON result on stdout; progress on stderr | writes explicit output/state; overwrite requires --force or resume | provider/network and separate budgets required unless offline fixture | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed distill resume` | `finetuning embed distill resume --config <path> [--input <path>] [--state <path>] [--json] [--dry-run]` | versioned --config; operation-specific checkpoint/artifact/report where shown | CLI > environment reference > config > default | one JSON result on stdout; progress on stderr | writes explicit output/state; overwrite requires --force or resume | provider/network and separate budgets required unless offline fixture | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed distill status` | `finetuning embed distill status --config <path> [--input <path>] [--state <path>] [--json] [--dry-run]` | versioned --config; operation-specific checkpoint/artifact/report where shown | CLI > environment reference > config > default | one JSON result on stdout; progress on stderr | read-only unless selected execution is explicitly run | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed models list` | `finetuning embed models list [--id <id>] [--json] [--quiet]` | none; --id narrows results | registry lock | one JSON result on stdout; progress on stderr | read-only unless selected execution is explicitly run | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed models info` | `finetuning embed models info [--id <id>] [--json] [--quiet]` | none; --id narrows results | registry lock | one JSON result on stdout; progress on stderr | read-only unless selected execution is explicitly run | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed models license` | `finetuning embed models license [--id <id>] [--json] [--quiet]` | none; --id narrows results | registry lock | one JSON result on stdout; progress on stderr | read-only unless selected execution is explicitly run | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed models compat` | `finetuning embed models compat [--id <id>] [--json] [--quiet]` | none; --id narrows results | registry lock | one JSON result on stdout; progress on stderr | read-only unless selected execution is explicitly run | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed recipes list` | `finetuning embed recipes list [--id <id>] [--json] [--quiet]` | none; --id narrows results | registry lock | one JSON result on stdout; progress on stderr | read-only unless selected execution is explicitly run | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed recipes show` | `finetuning embed recipes show [--id <id>] [--json] [--quiet]` | none; --id narrows results | registry lock | one JSON result on stdout; progress on stderr | read-only unless selected execution is explicitly run | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed recipes lock` | `finetuning embed recipes lock [--id <id>] [--json] [--quiet]` | none; --id narrows results | registry lock | one JSON result on stdout; progress on stderr | writes explicit output/state; overwrite requires --force or resume | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed train init` | `finetuning embed train init --config <path> [--checkpoint <path>] [--artifact <path>] [--report <path>] [--json] [--quiet] [--dry-run]` | versioned --config; operation-specific checkpoint/artifact/report where shown | CLI > environment reference > config > default | one JSON result on stdout; progress on stderr | read-only unless selected execution is explicitly run | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed train validate` | `finetuning embed train validate --config <path> [--checkpoint <path>] [--artifact <path>] [--report <path>] [--json] [--quiet] [--dry-run]` | versioned --config; operation-specific checkpoint/artifact/report where shown | CLI > environment reference > config > default | one JSON result on stdout; progress on stderr | read-only unless selected execution is explicitly run | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed train estimate` | `finetuning embed train estimate --config <path> [--checkpoint <path>] [--artifact <path>] [--report <path>] [--json] [--quiet] [--dry-run]` | versioned --config; operation-specific checkpoint/artifact/report where shown | CLI > environment reference > config > default | one JSON result on stdout; progress on stderr | read-only unless selected execution is explicitly run | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed train run` | `finetuning embed train run --config <path> [--checkpoint <path>] [--artifact <path>] [--report <path>] [--json] [--quiet] [--dry-run]` | versioned --config; operation-specific checkpoint/artifact/report where shown | CLI > environment reference > config > default | one JSON result on stdout; progress on stderr | writes explicit output/state; overwrite requires --force or resume | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed train resume` | `finetuning embed train resume --config <path> [--checkpoint <path>] [--artifact <path>] [--report <path>] [--json] [--quiet] [--dry-run]` | versioned --config; operation-specific checkpoint/artifact/report where shown | CLI > environment reference > config > default | one JSON result on stdout; progress on stderr | writes explicit output/state; overwrite requires --force or resume | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed train status` | `finetuning embed train status --config <path> [--checkpoint <path>] [--artifact <path>] [--report <path>] [--json] [--quiet] [--dry-run]` | versioned --config; operation-specific checkpoint/artifact/report where shown | CLI > environment reference > config > default | one JSON result on stdout; progress on stderr | read-only unless selected execution is explicitly run | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed train evaluate` | `finetuning embed train evaluate --config <path> [--checkpoint <path>] [--artifact <path>] [--report <path>] [--json] [--quiet] [--dry-run]` | versioned --config; operation-specific checkpoint/artifact/report where shown | CLI > environment reference > config > default | one JSON result on stdout; progress on stderr | read-only unless selected execution is explicitly run | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed train export` | `finetuning embed train export --config <path> [--checkpoint <path>] [--artifact <path>] [--report <path>] [--json] [--quiet] [--dry-run]` | versioned --config; operation-specific checkpoint/artifact/report where shown | CLI > environment reference > config > default | one JSON result on stdout; progress on stderr | writes explicit output/state; overwrite requires --force or resume | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed train inspect` | `finetuning embed train inspect --config <path> [--checkpoint <path>] [--artifact <path>] [--report <path>] [--json] [--quiet] [--dry-run]` | versioned --config; operation-specific checkpoint/artifact/report where shown | CLI > environment reference > config > default | one JSON result on stdout; progress on stderr | read-only unless selected execution is explicitly run | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed evaluate run` | `finetuning embed evaluate run --config <path> [--checkpoint <path>] [--artifact <path>] [--report <path>] [--json] [--quiet] [--dry-run]` | versioned --config; operation-specific checkpoint/artifact/report where shown | CLI > environment reference > config > default | one JSON result on stdout; progress on stderr | writes explicit output/state; overwrite requires --force or resume | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed evaluate compare` | `finetuning embed evaluate compare --config <path> [--checkpoint <path>] [--artifact <path>] [--report <path>] [--json] [--quiet] [--dry-run]` | versioned --config; operation-specific checkpoint/artifact/report where shown | CLI > environment reference > config > default | one JSON result on stdout; progress on stderr | read-only unless selected execution is explicitly run | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
| `embed evaluate inspect` | `finetuning embed evaluate inspect --config <path> [--checkpoint <path>] [--artifact <path>] [--report <path>] [--json] [--quiet] [--dry-run]` | versioned --config; operation-specific checkpoint/artifact/report where shown | CLI > environment reference > config > default | one JSON result on stdout; progress on stderr | read-only unless selected execution is explicitly run | offline; production download/GPU/remote capability remains gated | usage/config/schema, unavailable capability, policy/budget, checkpoint/artifact, provider/internal | versioned records/config/spec/events/artifacts reject incompatible majors |
