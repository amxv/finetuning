import assert from "node:assert/strict";import {test} from "node:test";
import {providerDistillation} from "../dist/cli/distill-provider.js";
import {RestRunPodLifecycleBackend} from "../dist/execution/runpod/lifecycle.js";
const config={runId:"r",salt:"s",generator:{provider:"openai",model:"g"},judge:{provider:"anthropic",model:"j"}};
test("provider CLI gates and injected adapters",async()=>{assert.throws(()=>providerDistillation(config,{network:false,generationCredentialEnv:"G",judgingCredentialEnv:"J",generationBudget:1,judgingBudget:1},{G:"x",J:"y"}),/NETWORK_OPT_IN/);assert.throws(()=>providerDistillation(config,{network:true,generationCredentialEnv:"G",judgingCredentialEnv:"J",generationBudget:1,judgingBudget:1},{}),/CREDENTIAL_MISSING/);let calls=0;const p=providerDistillation(config,{network:true,generationCredentialEnv:"G",judgingCredentialEnv:"J",generationBudget:1,judgingBudget:1},{G:"x",J:"y"},()=>({async invoke(){calls++;return {kind:"text",content:"ok"}}}));await p.generator.generate({requestId:"1",sampleId:"s",provider:"openai",model:"g",messages:[]});await p.generator.generate({requestId:"1",sampleId:"s",provider:"openai",model:"g",messages:[]});assert.equal(calls,1)});
test("REST lifecycle exact mutations and opt-in",async()=>{const calls=[];const transport={async request(path,init={}){calls.push([path,init]);if(path==="/pods"&&init.method==="POST")return {id:"p",name:"n",image:"sha",networkVolumeId:"v",desiredStatus:"RUNNING",env:{AMXV_OWNERSHIP_MARKER:"own",AMXV_SPEC_HASH:"spec"}};return []}};const input={name:"n",imageDigest:"sha",ownershipMarker:"own",specHash:"spec",volumeId:"v"};await assert.rejects(new RestRunPodLifecycleBackend(transport,false).createPod(input),/explicit allowLive/);const pod=await new RestRunPodLifecycleBackend(transport,true).createPod(input);assert.equal(pod.id,"p");assert.equal(calls[0][0],"/pods");assert.equal(calls[0][1].method,"POST");assert(!calls[0][1].body.includes("apiKey"))});
